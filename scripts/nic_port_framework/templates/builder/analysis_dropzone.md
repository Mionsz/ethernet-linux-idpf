# Analysis result drop-zone

Place completed analysis results here as JSON or Markdown. Generated source contexts and base prompts are immutable evidence; reviewed outputs are derived artifacts. Prefer the exact generated prompt basename for result naming.
