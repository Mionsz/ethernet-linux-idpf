## Result contract

Return one JSON object that validates against the `target` result schema. `target_item_id` must be
exactly `{{IDENTITY}}`.

## Scope discipline

This item is target-mandated work, not a translation. Do not restate source-driver behaviour that
belongs to a per-method record. Confine the analysis to the obligations and acceptance criteria
supplied with this item.

## Evidence discipline

Every claim about the source driver must name a real symbol, file or subsystem from this baseline.
Every claim about the target framework must name the interface or manual page it comes from. When
neither is available, record an open question rather than an assumption.

## Proposals

`port_requests` and `risks` are proposals only. Do not invent provenance fields; the orchestrator
assigns stable IDs, origin, scope and duplicate grouping.

Baseline fingerprint: `{{BASELINE_FINGERPRINT}}`.
